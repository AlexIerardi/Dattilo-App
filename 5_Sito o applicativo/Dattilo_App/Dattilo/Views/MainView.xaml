<Page x:Class="Dattilo.Views.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dattilo.Views"
      xmlns:viewmodels="clr-namespace:Dattilo.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">
    
    <Page.DataContext>
        <viewmodels:MainViewModel/>
    </Page.DataContext>
    
    <Page.Resources>
        <DataTemplate DataType="{x:Type viewmodels:ApprendimentoViewModel}">
            <local:ApprendimentoView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:DettaturaViewModel}">
            <local:DettaturaView/>
        </DataTemplate>
    </Page.Resources>
    
    <Grid>
        <ContentControl Content="{Binding SelectedViewModel}"/>
        <Button Content="Apprendimento" HorizontalAlignment="Left" Margin="276,197,0,0" VerticalAlignment="Top" Command="{Binding UpdateViewCommand}" CommandParameter="Apprendimento"/>
        <Button Content="Dettatura" HorizontalAlignment="Left" Margin="484,197,0,0" VerticalAlignment="Top" Command="{Binding UpdateViewCommand}" CommandParameter="Dettatura"/>
    </Grid>
</Page>
